# Copyright 2026 hrzlgnm
# SPDX-License-Identifier: MIT

# git-cliff configuration for generating CHANGELOG.md.

[changelog]
# changelog header
header = """
# Changelog\n
All notable changes to this project will be documented in this file.\n
The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).\n
"""
# template for the changelog body
body = """
{% if version -%}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }} {%- if previous and previous.version %} [compare](https://github.com/hrzlgnm/zux/compare/{{ previous.version }}...{{ version }}){% endif %}\n
{% else -%}\
    ## [Unreleased] {%- if commits and previous and previous.version %} [compare](https://github.com/hrzlgnm/zux/compare/{{ previous.version }}...HEAD){% endif %}\n
{% endif -%}\
{% for group, commits in commits | group_by(attribute="group") %}\
    {% if group == "Breaking" -%}\
        ### Breaking Changes\n
    {% elif group == "Added" -%}\
        ### Added\n
    {% elif group == "Fixed" -%}\
        ### Fixed\n
    {% elif group == "Changed" -%}\
        ### Changed\n
    {% elif group == "Dependencies" -%}\
        ### Dependencies\n
    {% elif group == "Security" -%}\
        ### Security\n
    {% elif group == "Maintenance" -%}\
        ### Maintenance\n
    {% elif group == "Miscellaneous" -%}\
        ### Miscellaneous\n
    {% endif -%}\
    {% for commit in commits -%}\
        - {% if commit.scope %}*({{ commit.scope }})* {% endif -%}\
            {{ commit.message | upper_first | trim }}\
            {% for link in commit.links %} ([{{ link.text }}]({{ link.href }})){% endfor %}\n
    {% endfor -%}\
{% endfor -%}\
"""
# remove the leading and trailing whitespace from the template
trim = true
# changelog footer
footer = ""
# postprocessors to apply to the changelog
postprocessors = [
    # fix a misspelling carried over from historical commit messages
    { pattern = 'throroughly', replace = 'thoroughly' },
    # remove duplicate (#NNN) PR refs already rendered as links ([#NNN](url))
    { pattern = '\s*\(#[0-9]+\)', replace = '' },
]

[git]
# parse the commits based on Conventional Commits
conventional_commits = true
# filter out unconventional commits
filter_unconventional = true
# process each line of a commit as an individual commit
split_commits = false
# regex for parsing and grouping commits
commit_parsers = [
    { message = "^feat!", group = "Breaking" },
    { message = "^fix!", group = "Breaking" },
    { message = "^refactor!", group = "Breaking" },
    { message = "^perf!", group = "Breaking" },
    { message = "^chore!", group = "Breaking" },
    { message = "^\\w+\\(ci\\)", group = "Maintenance" },
    { message = "^feat", group = "Added" },
    { message = "^fix\\(deps\\)", group = "Dependencies" },
    { message = "^fix", group = "Fixed" },
    { message = "^doc", group = "Changed" },
    { message = "^perf", group = "Changed" },
    { message = "^refactor", group = "Changed" },
    { message = "^style", group = "Changed" },
    { message = "^test", group = "Changed" },
    { message = "^ci", group = "Maintenance" },
    { message = "^build", group = "Maintenance" },
    { message = "^chore\\(deps\\)", group = "Dependencies" },
    { message = "^chore\\(renovate", skip = true },
    { message = "^chore\\(arch-aur", skip = true },
    { message = "^chore\\(void", skip = true },
    { message = "^chore\\(winget", skip = true },
    { message = "^chore\\(homebrew", skip = true },
    { message = "^chore\\(sbom", skip = true },
    { message = "^chore\\(signing", skip = true },
    { message = "^chore\\(bundler", skip = true },
    { message = "^chore\\(publish", skip = true },
    { message = "^chore\\(version", skip = true },
    { message = "^chore: update unreleased changelog", skip = true },
    { message = "^chore\\(android", skip = true },
    { message = "^chore\\(test", skip = true },
    { message = "^chore", group = "Changed" },
    { message = "^security", group = "Security" },
    { message = "^GHSA-", group = "Security" },
    { message = "^Bump version", skip = true },
    { message = ".*", group = "Miscellaneous" },
]
# protect breaking commits from being skipped
protect_breaking_commits = true
# filter out commits that are not matched by a commit_parsers
filter_commits = true
# regex for matching tags
tag_pattern = "^v[0-9].*"
# skip processing the matched tags
skip_tags = ""
# ignore processing the matched tags
ignore_tags = ""
# sort the tags topologically
topo_order = false
# sort the commits inside sections by oldest/newest order
sort_commits = "oldest"
# regex for extracting issue references
link_parsers = [
    { pattern = "#(\\d+)", text = "#${1}", href = "https://github.com/hrzlgnm/zux/pull/${1}" },
]
# glob patterns to include in the changelog
include_paths = []
# glob patterns to exclude from the changelog
exclude_paths = []
